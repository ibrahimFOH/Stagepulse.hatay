# DEPRECATED — Legacy Edge Function Source

Production source of truth is `supabase/functions/staff-manage/`.

This directory is intentionally retained for compatibility and historical reference.
Do not add new production logic here. Changes must be made under `supabase/functions/` first.

Migration status: compatibility layer / deprecated source.
