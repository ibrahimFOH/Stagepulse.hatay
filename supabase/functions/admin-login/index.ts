import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "npm:@supabase/supabase-js@2";
import { corsHeadersFor, handleOptions, getClientIp, isDistributedRateLimited, GENERIC_LOGIN_ERROR, jsonError } from "../_shared/security.ts";
Deno.serve(async(req)=>{const opt=handleOptions(req);if(opt)return opt;const headers=corsHeadersFor(req);if(req.method!=="POST")return jsonError("Method Not Allowed",405,headers);try{const body=await req.json().catch(()=>null);const username=typeof body?.username==="string"?body.username.trim().toLowerCase():"",password=typeof body?.password==="string"?body.password:"";if(!username||!password)return jsonError("Kullanıcı adı ve şifre zorunludur.",400,headers);const url=Deno.env.get("SUPABASE_URL")!,service=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!,anon=Deno.env.get("SUPABASE_ANON_KEY")!,admin=createClient(url,service,{auth:{persistSession:false}});const limitedByIp=await isDistributedRateLimited(admin,`admin-login:ip:${getClientIp(req)}`,10),limitedByAccount=await isDistributedRateLimited(admin,`admin-login:account:${username.slice(0,120)}`,10);if(limitedByIp||limitedByAccount)return jsonError("Çok fazla deneme yapıldı. Lütfen daha sonra tekrar deneyin.",429,headers);const isEmail=username.includes("@");const {data:legacy,error:le}=await admin.from("admin_profiles").select("user_id").eq("username",username).eq("active",true).maybeSingle();if(le)return jsonError("İşlem başarısız.",500,headers);if(!isEmail&&!legacy&&!/^[a-z0-9._-]{3,64}$/.test(username))return jsonError(GENERIC_LOGIN_ERROR,401,headers);let email=isEmail?username:`${username}@stagepulse.com.tr`;if(legacy?.user_id){const {data:legacyUser,error:legacyError}=await admin.auth.admin.getUserById(legacy.user_id);if(legacyError||!legacyUser.user?.email)return jsonError(GENERIC_LOGIN_ERROR,401,headers);email=legacyUser.user.email;}const auth=createClient(url,anon,{auth:{persistSession:false}}),{data,error}=await auth.auth.signInWithPassword({email,password});if(error||!data.session||!data.user)return jsonError(GENERIC_LOGIN_ERROR,401,headers);const target=data.user;const {data:membership,error:me}=await admin.from("org_memberships").select("active,role:role_id(code,name,tier,is_admin_role)").eq("user_id",target.id).eq("active",true).maybeSingle();const role=Array.isArray((membership as any)?.role)?(membership as any).role?.[0]:(membership as any)?.role;if(me||!membership||role?.is_admin_role!==true)return jsonError(GENERIC_LOGIN_ERROR,401,headers);return Response.json({session:data.session,user:{id:target.id,username:target.user_metadata?.username||username,display_name:target.user_metadata?.display_name||target.user_metadata?.full_name||"",role:role.code}},{headers});}catch(_){return jsonError("İşlem başarısız.",500,headers)}});
