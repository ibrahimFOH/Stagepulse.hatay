(() => { const items=[...document.querySelectorAll('section .section-title,section .section-desc,section .card,section .project-card')]; if(!items.length||!('IntersectionObserver' in window))return; items.forEach((el,i)=>{el.classList.add('sp-reveal');el.style.transitionDelay=`${Math.min(i%6,5)*55}ms`}); const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}}),{threshold:.12,rootMargin:'0px 0px -30px'});items.forEach(el=>io.observe(el));})();
