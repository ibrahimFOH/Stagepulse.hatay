# Stagepulse APK updater bootstrap

This marker intentionally triggers a fresh signed APK release after the updater and release synchronization fixes.

The release must contain the current `AppUpdater.kt` implementation, including Supabase-first update lookup, GitHub Release fallback, SHA-256 verification, pending-install resume, and Android package installation.

Bootstrap rebuild requested: 2026-08-29T21:10Z
